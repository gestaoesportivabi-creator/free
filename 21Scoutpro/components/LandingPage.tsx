/**
 * Re-export — implementação em ./landing/LandingPage (V2).
 * Mantém o import path estável para App / PublicApp.
 */
export { LandingPage } from './landing/LandingPage';
export type { LandingPageProps } from './landing/types';
